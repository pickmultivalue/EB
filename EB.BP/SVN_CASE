* @(#) SVN_CASE Ported to jBASE 07:23:52  18 FEB 2010
    FUNCTION SVN_CASE(Value)
!
! Return upper case of string if Windows
!
! This is (predominantly) for matching paths\items in SVN.LOCKS
!
    INCLUDE JBC.h
!
    IF DIR_DELIM_CH = '\' THEN rValue = UPCASE(Value) ELSE rValue = Value
!
    RETURN rValue
