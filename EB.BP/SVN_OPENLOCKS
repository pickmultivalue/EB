* @(#) SVN_OPENLOCKS Ported to jBASE 07:23:52  18 FEB 2010
    FUNCTION SVN_OPENLOCKS(F.Locks)
!
! OPEN 'SVN.LOCKS' and pass back the file variable
!
! Returns 1 if succussful, 0 if not
!
    INCLUDE JBC.h
    fname = 'SVN.LOCKS'
!
    OPEN fname TO F.Locks ELSE
        rc = GETENV('JBCRELEASEDIR', jbcreleasedir)
        fname = jbcreleasedir:DIR_DELIM_CH:'config':DIR_DELIM_CH:fname
        OPEN fname TO F.Locks ELSE
            RETURN 0
        END
    END
!
    RETURN 1
